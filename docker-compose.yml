version: '3.8'

services:
  utility-website:
    build: .
    ports:
      - "18080:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    container_name: utility-website